---
description: "記錄 skill 的 gotcha（踩過的坑）。觸發詞：gotcha、踩坑、不對、錯了、skill 問題、記個 gotcha"
allowed-tools: Read, Write, Edit, Glob, Grep, Bash(date:*), AskUserQuestion
---

# /gotcha — 記錄 Skill Gotcha

讀取 `${CLAUDE_PLUGIN_ROOT}/skills/gotcha/SKILL.md` 取得完整邏輯，然後執行。

使用者輸入：
$ARGUMENTS
