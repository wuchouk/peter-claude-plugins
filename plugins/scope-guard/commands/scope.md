---
description: "開發中的 scope check — 檢查改動是否在 engineering plan 範圍內，評估影響，管理優先級。觸發詞：scope、scope check、這個該做嗎、優先級、priority、現在做還是之後做、backlog、這重要嗎、偏離主線了嗎"
allowed-tools: Read, Write, Edit, Glob, Grep
---

# /scope — Scope Check

讀取 `${CLAUDE_PLUGIN_ROOT}/skills/scope/SKILL.md` 取得完整邏輯，然後執行。

使用者輸入：
$ARGUMENTS
