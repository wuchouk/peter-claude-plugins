---
description: "多 session 溝通協議 — 開啟/關閉溝通模式、跨 worktree 訊息收發。觸發詞：session-comms、開啟溝通模式、連到另一個 session、多 session 溝通、我要下線了、離開溝通模式、有誰在線、check who's online、connect to other sessions"
allowed-tools: Read, Write, Edit, Bash, Glob, Grep
---

# /session-comms — Multi-Session Coordination

讀取 `${CLAUDE_PLUGIN_ROOT}/skills/session-comms/SKILL.md` 取得完整協議，然後根據使用者輸入執行對應動作（開啟溝通模式、發訊息、讀 inbox、離線、列出在線 session 等）。

使用者輸入：
$ARGUMENTS
