---
description: "測試情境建構 — 記錄測試情境、記錄測試結果、整併優化。觸發詞：capture-test、測試情境、記錄測試、test scenario、test log、測一下、怎麼測、測試紀錄"
allowed-tools: Read, Write, Edit, Glob, Grep, Bash(date:*), Bash(mkdir:*), Bash(wc:*), AskUserQuestion
---

# /capture-test — 測試情境建構與管理

讀取 `${CLAUDE_PLUGIN_ROOT}/skills/capture-test/SKILL.md` 取得完整邏輯，然後執行。

## 用法

- `/capture-test` 或 `/capture-test record` — 記錄新的測試情境（模式 A）
- `/capture-test log` — 記錄測試結果（模式 B）
- `/capture-test optimize` — 整併優化既有情境（模式 C）

使用者輸入：
$ARGUMENTS
